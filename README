1. inside  /src is main.c
2. inside  /res is jpeg file 
3. inside lib* is the sub function used in main  


how to compile and use

mkdir build
cd build
cmake ..
sudo make 


/*
相对于4.0，本次主要在main.cpp中添加了部分代码，main中增加了判断每一帧图像的类型，
是谷物，刮板，还是空隙，这样有两个好处，第一是可以计算体积流量，第二是让自动计算速度成为可能。

and create serialSend.cpp,making serial send easier,and the main.cppeasy to read

*/
